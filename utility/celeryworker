[Unit]
Description=celery worker service
After=network.target

[Service]
User=${user}
Environment=PASSWORD=${postgres_pw}
ExecStart=${venvpath}/bin/celery  --workdir=${cwd} worker -A backend.celery:celery_app -l info

[Install]
WantedBy=multi-user.target

